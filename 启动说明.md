# ExcelAIFinder 启动说明

## 🎯 完成状态

✅ **已为您创建了完整的一键启动解决方案！**

## 🚀 可用的启动方式

### 1. Windows 批处理脚本（推荐）
```cmd
双击运行: start.bat
```
**特点**: 最简单，双击即可运行，自动检查环境并启动

### 2. PowerShell 脚本
```powershell
.\start.ps1
```
**特点**: 功能最全，带彩色输出，详细的进度信息

### 3. Shell 脚本（Linux/Mac）
```bash
./start.sh
```
**特点**: 跨平台兼容，支持Linux和Mac系统

### 4. NPM 命令
```bash
# 完整设置+启动
npm run dev

# 分别启动前后端
npm run server:dev  # 启动后端
npm run serve       # 启动前端（另一个终端）
```

## ⚙️ 已完成的自动化配置

1. ✅ **自动环境检查**: 检查Node.js（≥14.0.0）和npm版本
2. ✅ **自动配置文件创建**: 创建`restructure/server/.env`文件，端口设为3000
3. ✅ **自动依赖安装**: 安装前端和后端依赖（包括重构版后端）
4. ✅ **自动目录创建**: 创建uploads和logs目录
5. ✅ **端口冲突检测**: 检查3000和8080端口占用
6. ✅ **智能启动确认**: 启动前询问用户确认
7. ✅ **UTF-8编码支持**: 解决中文乱码问题

## 📝 使用步骤

### 第一次使用
1. **运行启动脚本**（选择上述任一方式）
2. **配置API密钥**：编辑 `restructure/server/.env` 文件，设置您的API密钥
   ```env
   # SophNet AI API配置 (用于语义分析)
   SOPHNET_API_URL=https://www.sophnet.com/api/open-apis/v1/chat/completions
   SOPHNET_API_KEY=your_sophnet_api_key_here
   SOPHNET_MODEL=DeepSeek-R1
   SOPHNET_MAX_TOKENS=32768
   ```
3. **重新启动**：再次运行启动脚本

### 日常使用
- 直接双击 `start.bat`
- 或运行 `npm run dev`
- 或分别启动：`npm run server:dev` + `npm run serve`

## 🌐 访问地址

启动成功后，浏览器访问：
- **前端界面**: http://localhost:8080
- **后端API**: http://localhost:3000
- **API健康检查**: http://localhost:3000/api/health

## 📋 当前配置

- ✅ **前端端口**: 8080（Vue开发服务器）
- ✅ **后端端口**: 3000（Express服务器）
- ✅ **后端架构**: 使用重构版模块化架构（restructure/server/）
- ✅ **API地址**: https://www.sophnet.com/api/open-apis/v1/chat/completions
- ✅ **AI模型**: DeepSeek-R1
- ⚠️ **API密钥**: 需要用户手动配置（配置文件：`restructure/server/.env`）

## 🔧 故障排除

### 端口被占用
```bash
# Windows: 查找并杀死占用端口的进程
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# 或修改配置文件中的端口
# 编辑 restructure/server/.env
PORT=3001
```

### API连接失败
- ✅ 确认已配置正确的API密钥
- ✅ 检查网络连接
- ✅ 确认API密钥有效且有余额
- ✅ 查看 `restructure/server/.env` 文件配置

### 依赖安装失败
```bash
# 清理重装前端依赖
npm cache clean --force
rm -rf node_modules package-lock.json
npm install

# 清理重装后端依赖
cd restructure/server
rm -rf node_modules package-lock.json
npm install

# 使用国内镜像
npm config set registry https://registry.npmmirror.com/
```

### 启动脚本问题
- **乱码问题**: start.bat已添加`chcp 65001`支持UTF-8
- **权限问题**: Linux/Mac用户需要`chmod +x start.sh`
- **PowerShell执行策略**: 可能需要`Set-ExecutionPolicy RemoteSigned`

## 📂 项目结构

```
ExcelAIFinder/
├── src/                      # 前端Vue应用
├── server/                   # 原版后端（备用）
├── restructure/server/       # 重构版后端（推荐使用）
│   ├── .env                  # 环境配置文件 ⭐
│   ├── config/               # 配置文件
│   ├── controllers/          # 控制器
│   ├── routes/               # 路由定义
│   └── ...
├── start.bat                 # Windows启动脚本
├── start.ps1                 # PowerShell启动脚本
├── start.sh                  # Linux/Mac启动脚本
└── README.md                 # 详细说明文档
```

## 🎉 总结

**您现在拥有了4种便捷的启动方式！**

- **最简单**: 双击 `start.bat`（Windows）
- **最详细**: 运行 `.\start.ps1`（PowerShell）
- **跨平台**: 运行 `./start.sh`（Linux/Mac）
- **开发者**: 使用 `npm run dev`（命令行）

### 📋 使用检查清单

- [ ] Node.js ≥ 14.0.0 已安装
- [ ] npm ≥ 6.0.0 已安装
- [ ] 运行启动脚本完成初始化
- [ ] 配置 `restructure/server/.env` 中的API密钥
- [ ] 访问 http://localhost:8080 确认前端正常
- [ ] 访问 http://localhost:3000/api/health 确认后端正常
- [ ] 测试文件上传功能

### 🔗 更多信息

- 📖 **详细文档**: 查看 `README.md`
- 🏢 **公司信息**: 厦门佰能思维人工智能科技有限公司
- 📧 **联系邮箱**: 538825006@qq.com
- 🐛 **问题反馈**: https://github.com/haclon/ExcelAIFinder/issues

**所有脚本都会自动完成环境设置，您只需要配置API密钥即可开始使用！** 🚀 